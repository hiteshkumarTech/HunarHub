# Base URL of the HunarHub API. Optional locally (defaults to the live Render
# backend). In Vercel, set this in Project → Settings → Environment Variables.
VITE_API_URL=https://hunarhub-api-s03k.onrender.com
